# clickhousephp Extension

Auto-generated PHP extension from Go code.

## Functions

### clickhouse_connect

```php
clickhouse_connect(string $dsn): string
```

**Parameters:**

- `dsn` (string)

**Returns:** string

### clickhouse_disconnect

```php
clickhouse_disconnect(): string
```

**Returns:** string

### clickhouse_insert

```php
clickhouse_insert(string $table,array $values,array $columns): string
```

**Parameters:**

- `table` (string)
- `values` (array)
- `columns` (array)

**Returns:** string

### clickhouse_exec

```php
clickhouse_exec(string $query): string
```

**Parameters:**

- `query` (string)

**Returns:** string

### clickhouse_query_array

```php
clickhouse_query_array(string $query): array
```

**Parameters:**

- `query` (string)

**Returns:** array


